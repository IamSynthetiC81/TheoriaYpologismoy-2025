const int N = 100;
int a[100];
int main() {
    for (int i = 0; i < N; ++i) {
        a[i] = i;
    };
    half = (float *)malloc(100 * sizeof(float));
    for (int i = 0; i < 100; ++i) {
        half[i] = x / 2;
    }
}
